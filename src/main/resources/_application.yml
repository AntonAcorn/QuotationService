spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:quoteservice
    password: password
    username: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect